- **[invariant]** A DISCOVERED connection is session-only and never persisted: connections.save is a WHITELIST (only source == 'file' is written), discovery.to_spec asserts the spec carries no password/password_env/password_cmd, and a password read from a .env or compose file lives in app state.secrets, handed to the session via session.new(spec, options, { secret }). state.snapshot also skips a discovered connection so restore cannot point at one.  _(2026-08-15T09:51:24Z)_
- **[pitfall]** Opening dblens with zero connections now RUNS a workspace scan (discovery.auto), so any spec that calls app.open() must set discovery = { auto = false } in its options or the suite scans the developer's filesystem and can pop a picker mid-test.  _(2026-08-15T09:51:24Z)_
- **[pitfall]** Database magic headers: sqlite is 'SQLite format 3\0' at byte 1; duckdb writes an 8-byte checksum FIRST, so its 'DUCK' magic starts at byte 9. The header beats the extension, which is how an ambiguous .db is classified.  _(2026-08-15T09:51:24Z)_
