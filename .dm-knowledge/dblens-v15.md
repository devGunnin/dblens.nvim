- **[pitfall]** Catalog column metadata (types) is loaded only when the tree EXPANDS a relation (loader.relation_details); opening a table straight into the grid leaves catalog:info_for(rel).columns nil. Anything type-aware (grid alignment, the filter builder's menu order) must tolerate nil and fall back, not assume a type.  _(2026-08-15T18:12:15Z)_
- **[pitfall]** results.current_cell passes the 0-based cursor byte column straight to vim.fn.virtcol(), which wants 1-based, so the reported virtcol is one short: with the cursor on the FIRST character of a column, nearest_column falls back to the PREVIOUS column. Verified live driving the real grid. Affects every cell action (F/s/gf/e), not just filtering.  _(2026-08-15T18:12:16Z)_
