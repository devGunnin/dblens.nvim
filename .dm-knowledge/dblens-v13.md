- **[invariant]** discovery.env reads a variable group by DECOMPOSING its name into [<prefix>_]<family>_<role>: family is the engine word (POSTGRES/PG/MYSQL/...) or the engine-less DB/DATABASE, whose values fill the gaps of the one engine group under the same prefix. A bare PORT/HOST/NAME resolves to nothing on purpose - those are the web server's. libpq's glued names (PGHOST, PGDATABASE...) and MYSQL_TCP_PORT are an explicit ALIASES table, never a general glue rule.  _(2026-08-15T15:16:28Z)_
- **[convention]** sql.page's order_by is a LIST of { column, desc } sort keys (v1.3), not one key; common.order_keys builds the quoted key list and mssql.sql.page shares it instead of repeating the ORDER BY logic. state.grid.sort is nil or a non-empty list of the same shape.  _(2026-08-15T15:16:28Z)_
- **[pitfall]** The grid header text is the column name PLUS its sort marker, so render.grid measures column widths from the decorated header, not from input.columns - measuring the bare name left the arrow overflowing into the separator.  _(2026-08-15T15:16:28Z)_
