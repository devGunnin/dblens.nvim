- **[invariant]** The dblens UI now HIDES rather than tears down on q/toggle: state (session, tabs, tree, SQL buffer) survives because the pane BUFFERS survive - layout.hide drops the windows only, layout.show rebuilds windows over the same bufs. Never re-run the pane attach() on show: editor.attach rewrites the buffer with the welcome text. app.close() is the only teardown.  _(2026-08-15T17:11:52Z)_
- **[invariant]** Boot must never error on a bad connection: connections.health(spec) resolves the REFERENCE only (env var, validation, file presence) and never runs a password_cmd or opens a database; open/restore skip a broken connection with ONE WARN line. Secrets still resolve only in session:connect.  _(2026-08-15T17:11:52Z)_
