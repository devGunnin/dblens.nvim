- **[pitfall]** column.fk is NOT one shape: sqlite/postgres/mysql/mariadb/mssql emit 'table.column' (sqlite COALESCEs a NULL 'to' to an empty column, meaning the implicit PK; sqlite/mysql group_concat several into one comma list), but duckdb emits the raw constraint text 'FOREIGN KEY (a) REFERENCES t(x)' because duckdb_constraints() is the only place it holds the fact. lua/dblens/fk.lua parses both.  _(2026-08-15T11:48:30Z)_
- **[pitfall]** A GENERATED predicate (filter-from-cell, FK nav) whose cell value contains a ';' is refused on a LOCKED connection no matter how correctly it is quoted: sql.single_statement_problem is a byte scan that deliberately does not read quoting, so it cannot tell a ';' inside a literal from a stacked statement. app checks for it up front and names the value; unlocking is the only way through. check_predicate does NOT catch this - it tokenises, so a ';' inside a string token passes.  _(2026-08-15T11:48:30Z)_
- **[convention]** loader.part skips its query entirely when the catalog already holds that part, so a spec can pre-seed catalog:set_part(relation, 'columns'|'indexes'|'constraints', ...) and load_relation_details then makes ZERO client calls - which is what makes catalog-driven flows (FK nav) deterministic to test.  _(2026-08-15T11:48:30Z)_
- **[pitfall]** tests/helpers.with_fake_exec used to restore only the module entries it had SAVED as non-nil, so a dependent that was not loaded when the double went in (commonly dblens.session) stayed loaded bound to the STUB - a later 'live' case then passed with no database at all. It now restores by name, nil included; keep it that way when adding a module to EXEC_DEPENDENTS.  _(2026-08-15T13:31:12Z)_
- **[invariant]** state.grid is a POINTER to the active tab in state.tabs.list; app.activate is the only place it is repointed. Every async caller captures the grid table it dispatched for and checks that grid's own fetch/epoch token - the guards are per tab, so a slow query cannot write into another tab.  _(2026-08-15T13:31:12Z)_
- **[convention]** CSV quoting carries the NULL distinction: an UNQUOTED empty field is SQL NULL, a quoted one is the empty string. export.csv_field writes it that way and dblens.csv reads it back, matching sqlite3/psql/COPY CSV; anything that changes one side breaks the export-import round trip.  _(2026-08-15T13:31:12Z)_
- **[pitfall]** catalog:all_relations() walks schema_list(), which on a schema-ful engine (postgres/mysql/mssql) is EMPTY until loader.schemas has run. Anything scanning the whole catalog (reverse FK) must load schemas first, or it silently sees no tables.  _(2026-08-15T13:31:13Z)_
