- **[pitfall]** sqlite3 emits NO output at all for a zero-row query, not even a header row. Decoding must accept caller-supplied column names (protocol.decode opts.columns) or a zero-row table loses its headers.  _(2026-08-14T16:49:10Z)_
- **[pitfall]** changes() (sqlite) and ROW_COUNT() (mysql) report on the CLIENT SESSION, and every dblens call spawns a fresh client process. An affected-row query issued afterwards always answers 0; it must be appended to the write in the SAME invocation (session:with_affected).  _(2026-08-14T16:49:10Z)_
- **[pitfall]** A dialect's `backtick`/`bracket` flags mean 'the lexer must RECOGNISE this quoting'; `ident_quote` is what we EMIT. sqlite accepts backticks but must be quoted with double quotes.  _(2026-08-14T16:49:10Z)_
- **[invariant]** EVERY statement goes through session:run, which calls session:gate first. The gate owns the destructive-confirm and the refusal of client meta-commands, and it refuses writes early on a read-only connection for a good message - but it is NOT the read-only guarantee (see the server-side note). An earlier form of this note said `execute_write` and was FALSE: ad-hoc editor SQL called run_script directly and skipped it entirely.  _(2026-08-14T16:49:10Z)_
- **[pitfall]** Nerd Font glyphs (private use area) did not survive being written into a source file and silently became empty strings. ui/icons.lua writes them as decimal UTF-8 byte escapes; default icon set is plain Unicode.  _(2026-08-14T16:49:10Z)_
- **[routing]** All three adapters are verified live: sqlite3 3.53, PostgreSQL 18.4, MySQL 8.4 (the last via a `mysql:8.4` container; there is no mysql client on this machine, so drive it through a wrapper script that `docker exec -i`s into the container and forwards argv + MYSQL_PWD). mysql's sql.assert_one row guard was executed, not reasoned: guard=1 returns 1, guard=2 gives ER_SUBQUERY_NO_1_ROW at a line exec.error_line can read, nothing applied.  _(2026-08-14T16:49:10Z)_
- **[convention]** Transactions are deferred batches, not held sessions: CLI clients are one-shot, so changes queue and commit replays them in one BEGIN/COMMIT. A queued change is NOT visible to the server until commit - the grid marks it pending rather than pretending it landed.  _(2026-08-14T16:49:10Z)_
- **[command]** Test: `make test` runs nvim --headless --noplugin -u tests/minit.lua with MiniTest.run wrapped in pcall. The pcall wrapper is required: a spec that fails to LOAD makes a bare MiniTest.run() hang headless nvim forever instead of exiting nonzero.  _(2026-08-14T16:49:19Z)_
- **[command]** Lint: `make lint` runs stylua --check . and luacheck lua tests. .luacheckrc must declare vim under `globals`, not `read_globals`, or every vim.bo/vim.wo assignment is flagged.  _(2026-08-14T16:49:19Z)_
- **[convention]** mini.test is pinned to tag v0.18.0 and cloned into .tests/ on first run only; every later run is offline. Specs live in tests/spec/*_spec.lua and are collected automatically.  _(2026-08-14T16:49:19Z)_
- **[invariant]** READ-ONLY IS ENFORCED SERVER-SIDE, per adapter, in `command()` off `spec.read_only`: sqlite `-readonly`, postgres `PGOPTIONS=-c default_transaction_read_only=on`, mysql `--init-command=SET SESSION TRANSACTION READ ONLY`. Never move this back into the Lua lexer. Three reviews broke the classifier because dblens's lexer and the server's parse differently and every divergence is a bypass; the server's own parser cannot disagree with itself. `readonly_spec` deliberately BYPASSES session:gate to prove it - a test that asks the classifier first proves nothing about the case the classifier got wrong.  _(2026-08-14T00:00:00Z)_
- **[invariant]** sql.classify is now BEST-EFFORT UX: it decides whether to prompt, not whether a write is allowed. Still biased closed (unknown verb, stacked script, EXPLAIN ANALYZE => write), because on a WRITABLE connection the prompt is the only thing left. `WITH ... DELETE`, `COPY`, `DO`, `CALL`, `SELECT ... INTO`, `PRAGMA x = v` all lead with a read verb and must stay writes.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** Block comments nest ONLY in postgres. `/* /* */` is a complete comment in sqlite/mysql/standard, so a lexer that nests unconditionally swallows the closing `*/` and every statement after it - `SELECT 1 /* /* */ ; DROP TABLE t` read as one SELECT here and dropped the table on both. Dialect flag `nested_comment`; permissive must be FALSE, because the fail-safe direction for a scanner is to consume LESS.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** MySQL/MariaDB RUN the body of `/*!40000 ... */` and `/*M! ... */`. Lexing it as a comment made `classify` see zero tokens and answer "nothing to run". It is emitted as its own `exec_comment` token type, kept in `tokens()` and NOT blanked by `strip()`; `analyse` allow-lists token types so any unmodelled span fails closed.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** Whether `\` escapes inside a mysql literal is a SERVER setting (`sql_mode=NO_BACKSLASH_ESCAPES`), not a dialect fact. Under it the lexer closed the literal later than the server did and `SELECT 'a\'; DROP TABLE payroll; -- '` dropped the table. Reproduced live on 8.4. A string token containing `\` under a backslash_escape dialect is left unprovable - the same rule common.check_predicate already used.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** The `mysql` client honours bare `system` / `source` at the head of a line from STDIN in batch mode - `system` shells out, `source` reads and executes a local file. Verified: `--skip-named-commands` does NOT stop either, and a read-only session cannot (they never reach the server). Only sql.client_meta_problem can refuse them.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** `sqlite3 -cmd '<sql>'` runs the command and then NEVER READS STDIN - the whole piped script is silently skipped, exit 0. So a session-head PRAGMA cannot be passed that way; `-init <file>` does work. `-readonly` is preferred over `PRAGMA query_only` anyway: query_only is session state a smuggled `PRAGMA query_only=OFF` can undo, the open mode cannot be.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** A write verb is only a hidden statement where a statement can BEGIN - after `(`, `)` or `;`, and not when followed by `(`. Sweeping for the bare word refused `SELECT REPLACE(...)`, `SELECT TRUNCATE(x,2)`, a column called `comment` and `FOR UPDATE`. Shared rule: `sql.opens_statement(tokens, index)`, used by both analyse and check_predicate. Non-ASCII bytes must extend a word token or an unquoted `größe` falls through to punct and the read is refused.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** exec kills the client when stdout hits max_bytes, so the result is `ok=false, reason='max_bytes', truncated=true`. session:run treated ANY truncated result as success so a partial read still renders - which reported "committed N change(s)" for a batch the server had rolled back. A truncated result is only a success for a READ; session:run knows which from the gate's classification.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** `vim.fn.expand()` performs Vim wildcard expansion, which RUNS BACKTICKS through &shell. A connections file with a backtick in `path` was remote code execution on open. Use `vim.fs.normalize` (verified: does not evaluate) plus an explicit reject-list; `vim.fn.fnamemodify` is safe.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** `sqlite3` without `-bail` PRINTS the error, SKIPS the failing statement and still runs the trailing COMMIT, so a BEGIN/COMMIT batch commits in part. Reproduced. `-bail` fixes it; psql needs `-v ON_ERROR_STOP=1` (verified live on 18.4). Wrapping in BEGIN/COMMIT is not what makes a batch atomic.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** `-batch` does NOT disable sqlite3 dot-commands: `.shell` still runs. `-safe` (sqlite3 >= 3.34) refuses `.shell`, `.load`, `.import` and ATTACH while leaving ordinary writes and file creation working. Verified.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** psql's unaligned output does not escape control bytes, so a value holding 0x1F/0x1E split the row or the record. Reproduced live. `--csv` quotes anything ambiguous; a quoted field is always literal text, so only an unquoted field can be the NULL marker. Residual: a value that is exactly the NULL byte is still indistinguishable from NULL.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** psql only prefixes an error with `psql:<stdin>:LINE:` when it reads with `-f -`. Piping to plain stdin gives no line number, so a failed statement inside a committed batch cannot be traced back to the change that produced it.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** `vim.system{ text = true }` rewrites CRLF to LF in the collected output, which corrupts a record stream before decoding. Use `text = false`.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** sqlite has no RAISE outside a trigger and returns NULL for `1/0`, so neither aborts a batch. `json('<invalid>')` raises "malformed JSON" and does abort under `-bail`; that is how the row guard is emitted into a committed transaction. Postgres uses a DO block with RAISE EXCEPTION.  _(2026-08-14T00:00:00Z)_
- **[pitfall]** app.lua's async callbacks read a module-level `state` upvalue. Closing during a query nil'd it before `spinner:stop()` ran its frame callback, which threw and skipped every later cleanup step - leaving a visible layout that `is_open()` reported as closed, so no later close could reach it. Stop the spinner BEFORE dropping state, and bind every async callback to the instance that started it.  _(2026-08-14T00:00:00Z)_
- **[convention]** tests/helpers.with_fake_exec swaps `dblens.exec` for a scripted double and reloads session/connections/app, so the write path, cancellation and close-during-query are testable with no database. `pending = true` holds a call open; `call.resume(...)` delivers it.  _(2026-08-14T00:00:00Z)_
- **[invariant]** A LOCKED connection sends exactly ONE statement. sql.single_statement_problem is a BYTE SCAN (no lexer): refuses a ';' anywhere but the final char, any control byte except tab/newline, and U+0085/U+2028/U+2029. Reason: psql ends a '--' comment at a bare \r and dblens's lexer did not, so a stacked DROP landed on a locked pg connection. Never replace this with a lexer rule - the read-only transaction makes ONE statement unescapable, this makes a SECOND unreachable. Cost: while locked, a ';' inside a string literal or quoted identifier is refused too.  _(2026-08-14T20:01:32Z)_
